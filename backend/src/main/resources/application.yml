server:
  port: 8080
  address: 0.0.0.0  # 建议保留（解释见下）

spring:
  application:
    name: ChatApp

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/chat_app
    username: root
    password: 123456

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 100MB

mybatis:
  configuration:
    map-underscore-to-camel-case: true